<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<body>
  <%- include("header.ejs") %>
  <% for(let i = 0; i < result.length; i++){ %>
    <div class="container text-center">
      <div class="row justify-content-start">
      <div class="col">
      <p class="lead"> Chapter <%= result[i].chapter_number %></p>
      <p class="lead"> <%= result[i].name %></p>
      <p class="lead">  <%= result[i].name_meaning %></p>
      <p class="lead">  <%= result[i].chapter_summary %></p>
      </div>
      </div>
    </div>
  <% } %>
  
</body>
</html>