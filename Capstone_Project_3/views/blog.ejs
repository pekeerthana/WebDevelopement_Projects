<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<body>
  <%- include('header.ejs') %>
  <% if (blog) { %>
  <div class="container">
     <div class="p-5 text-center bg-body-tertiary rounded-3">
       <svg class="bi mt-4 mb-3" style="color: var(--bs-indigo);" width="100" height="100" aria-hidden="true">
        <use xlink:href="#bootstrap"></use></svg>
         <h1 class="text-body-emphasis"><%= blog.title %></h1>
          <p class="col-lg-8 mx-auto fs-5 text-muted">
            <%= blog.content %>
          </p>
          <p><small>Posted on <%= new Date(blog.createdAt).toLocaleDateString() %></small></p>
          <div class="d-grid gap-2 d-sm-flex justify-content-sm-center py-3">
            <form action="/edit" method="get">
              <input type="hidden" name="id" value="<%= blog.id %>" />
              <button type="submit" class="btn btn-primary px-4 gap-3">Edit</button>
            </form>
            <form action="/delete" method="POST">
              <input type="hidden" name="id" value="<%= blog.id %>" />
              <button type="submit" class="btn btn-danger px-4">Delete</button>
            </form>
            </div>
         </div>
    </div>
    <% } else { %>
      <h1>Blog Not Found</h1>
      <p>The blog you are looking for does not exist.</p>
    <% } %>
  
</body>
</html>